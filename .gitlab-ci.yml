image: node:latest

cache:
  key: node_modules
  paths:
    - node_modules

stages:
  - github
  - install
  - build
  - deploy

github:
  stage: github
  script:
    - git clone --bare https://gitlab.com/my-portfolio-exprerience/my-portfolio-v3.git
    - cd my-portfolio-v3.git
    - git push --mirror https://$GITLAB_USERNAME@github.com/st-nann/my-portfolio-v3.git
    - cd ..
    - rm -rf my-portfolio-v3.git

install:
  stage: install
  only:
    - master
  script:
    - yarn install
  artifacts:
    paths:
      - node_modules

build:
  stage: build
  only:
    - master
  script:
    - yarn build:production
  dependencies:
    - install
  artifacts:
    paths:
      - dist

deploy:
  stage: deploy
  only:
    - master
  script:
    - yarn global add firebase-tools --cache-folder node_modules
    - firebase deploy -m "Pipeline $CI_PIPELINE_ID, build $CI_BUILD_ID" --non-interactive --token $FIREBASE_DEPLOY_KEY --project $FIREBASE_PROJECT
  dependencies:
    - install
    - build
